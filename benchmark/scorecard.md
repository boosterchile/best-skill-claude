# Scorecard — `<YYYY-MM-DD>` (window: <since> → <until>)

> Generated by `bash benchmark/scripts/score-session.sh` and
> `bash benchmark/scripts/compare-to-baseline.sh`. This file is the human-readable
> template; the scripts produce something matching this shape.

## Headline

- Sessions in window: N
- Phase exits: N
- Features touched: N
- Shipped: N
- Drift detected / blocked / justified: N / N / N

## Compliance metrics (vs target and baseline)

```
Metric                                Current     Target    Without   Δ vs target   Status
------                                -------     ------    -------   ------------  ------
spec before code rate                     —%         95%        20%         —pp      ?
tdd compliance                            —%         80%        15%         —pp      ?
commits le 100 loc                        —%         90%        55%         —pp      ?
review before merge rate                  —%        100%        40%         —pp      ?
devils advocate invocation rate           —%        100%         0%         —pp      ?
adr coverage                              —%         80%        10%         —pp      ?
skip cycle rate                           —%         15%        45%         —pp      ?
cooling off respect rate                  —%         95%         0%         —pp      ?
phase artifact completeness               —%         95%        30%         —pp      ?
drift block to detect ratio               —%         70%         0%         —pp      ?
```

Status legend: ✓ at-or-above target | ⚠ above baseline but below target | ✗ at-or-below baseline

## Drift

- Top triggers (descending count):
  - `<phrase>`: N
  - `<phrase>`: N
- Block → detect ratio: N / N = N%
- Justifications (most common rationale):
  - `<reason class>`: N

## Skills

- Most read this window:
  - `11-spec-driven-development`: N reads
  - `<skill>`: N reads
- **Not read** this window (despite touching relevant code):
  - `52-security-and-hardening` — but auth changes occurred
  - `<skill>` — but `<area>` was modified

## Phases

- /spec invocations: N (avg time to approval: N min)
- /plan invocations: N
- /build commits: N
- /test invocations: N
- /review invocations: N (cooling-off respected: N / N)
- /ship invocations: N

## Waivers and skip-cycles

- `[skip-cycle]` count: N — reasons:
  - `<reason summary>` × N
- `[waiver]` count: N — reasons:
  - `<reason summary>` × N
- `[drift_justified]` count: N — top phrases justified:
  - `<phrase>` × N

## Notable events

- (auto-extracted from ledger; cap at 10)
- `<timestamp>`: <event type> — <one-line summary>
- ...

## Suggested actions

(At most 2-3, ranked by impact. Generated heuristically; treat as starting points, not commands.)

1. **<metric/area>** — `<concrete next action this week>`. Reason: `<one line>`.
2. **<metric/area>** — `<concrete next action this week>`. Reason: `<one line>`.

## Notes for the next retro

(Free-form for the user to fill in before the quarterly review. The script does not write this section.)

-
-
-
